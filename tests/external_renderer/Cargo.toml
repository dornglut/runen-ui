[package]
name = "runenui_external_renderer_conformance"
version.workspace = true
description = "Downstream renderer-neutral scene consumer conformance fixture for RunenUI"
readme = "README.md"
repository.workspace = true
license.workspace = true
publish = false
edition.workspace = true
rust-version.workspace = true

[dependencies]
runenui_core = { path = "../../crates/runenui_core", version = "0.1.0" }
runenui_runtime = { path = "../../crates/runenui_runtime", version = "0.1.0" }

[lints]
workspace = true
