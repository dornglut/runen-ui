# Validation

`cargo validate` is the single maintained repository validation authority. It covers repository policy, links, locked metadata and dependency trees, formatting, workspace tests, downstream conformance, Clippy, rustdoc, Rust 1.93.0 tests, diff hygiene, and clean tracked state.
