# Numerical Contract

Every successful sample contains a finite signed value and an optional finite non-negative conservative tracing step. Sphere tracing consumes only the conservative step. Exact-distance capability is explicit.
