# M6 Conformance Matrix

> **Category: Target architecture**
>
> **Status:** Accepted
>
> **Milestone:** M6
>
> **Reviewed baseline:** `8e09a61832e2077db0e1366472b628c9b2478880`
>
> **Acceptance condition:** This matrix becomes normative only when the exact
> M6A0 architecture/conformance package containing it is explicitly accepted by
> the repository owner and merged. That merge still does not authorize M6
> implementation: the bounded M6A0 current-contract reconciliation must also be
> accepted, merged, and accepted-main validated before M6A may begin.

This matrix is the single M6-specific observable behavior and proof inventory.
[ADR 0007](../adr/0007-renderer-neutral-paint-hit-scene-protocol.md) owns the
renderer-neutral scene/publication architecture. The accepted M4 matrix continues
to own exact displayed-generation surface input, including `SURFACE-*`; the
accepted M5 matrix continues to own semantic independence and staged publication
atomicity. Those observations are referenced here where inherited but are not
duplicated with new IDs.

M6A0 owns architecture/conformance authority only and implements no scene
behavior. Every M6 behavior row therefore begins `blocked`. Acceptance of M6A0
freezes the contract and implementation sequence; it does not promote any row.

```text
36 total unique rows
29 owner-accepted
0 implementation-complete
0 proof-complete
7 blocked
0 duplicate IDs
0 invalid statuses
0 invalid schemas
```

## Row contract and completion rule

Every ID is permanent. New observations append the next zero-padded number in
that family; IDs are never recycled because implementation moves. Allowed
statuses retain the repository meanings:

- `blocked`: the owning implementation slice has not been accepted;
- `implementation-complete`: public behavior exists but the complete proof
  package has not passed;
- `proof-complete`: exact-head positive/negative/diagnostic or trace proof and
  validation pass, but owner acceptance and merge remain pending;
- `owner-accepted`: public behavior, complete proof, validation, critical review,
  explicit owner acceptance, guarded merge, content identity, and required
  accepted-main validation have passed.

`Required` means the row must be `owner-accepted` before M6 closes. M6A0 has no
behavior rows. Delivery slices are fixed by ADR 0007:

- M6A — persistent retained-publication substrate (#59);
- M6B — canonical paint/hit scene kernel and displayed-hit cutover;
- M6C — transforms, clips, resources, metadata, damage, and capabilities;
- M6D — independent consumers, migration, and M6 closure.

Proof through a test-only parallel scene, forged target/generation/revision,
private callback bridge, compatibility alias, or backend-specific alternate
scene is not valid conformance.

## M6A — persistent retained-publication substrate

| ID | Required observation | Positive proof owner | Negative proof owner | Diagnostic / trace proof owner | Delivery slice | Status | Gate |
|---|---|---|---|---|---|---|---|
| SCENE-PUB-01 | Non-structural planning starts from cheap immutable/shared retained phase products and does not deep-clone the complete surface cache before deciding dirty replacements. | Narrow-publication retained-product reuse tests and allocation/copy characterization | Whole-`SurfaceCache` clone regression proof across clean, focus-only, semantic-only, style-only, layout, and paint cases | Publication phase diagnostics/benchmark evidence | M6A | owner-accepted | Required |
| SCENE-PUB-02 | Unchanged topology/style/layout/hit/paint/diagnostic products are reused while each dirty phase owns an explicit replacement; deterministic output order is independent of the chosen storage handles; the retained layout phase remains the single storage owner from which runtime derives current directional-focus geometry without a second focus cache. | Per-phase handle/reuse and dirty-replacement tests plus current-focus geometry projection/reuse proof | Cross-phase accidental replacement, storage-order iteration, alias-to-mutable-state, duplicate focus cache, and hit-scene-as-focus-geometry tests | Surface phase report plus retained-product/focus-projection identity diagnostics | M6A | owner-accepted | Required |
| SCENE-PUB-03 | The retained-storage migration preserves accepted M5 `SEM-PUB-04`: rejected/backpressured/counter-exhausted/integrity-failed plans commit zero new cache, semantic, displayed-hit, redraw, rehit, or success-trace state. | Existing M5 atomicity corpus rerun against new substrate plus M6 narrow-plan cases | Partial-swap, rollback-copy, lost-reservation, and terminal-wrap regression proof | Inherited M5 publication trace/counter evidence | M6A | owner-accepted | Required |
| SCENE-PUB-04 | Semantic publication remains an independently typed sibling coordinated by the same final surface commit; persistent renderer phase storage never becomes semantic identity/tree authority. | Public semantic-versus-renderer product independence proof after storage migration | Semantic facts in renderer cache/scene and renderer facts in semantic product audit | Cross-product publication diagnostics and M5 semantic trace regression | M6A | owner-accepted | Required |
| SCENE-PUB-05 | Structural and dirty-phase publications remain deterministic while narrow clean/focus/semantic publications demonstrate no O(surface) deep copy of every retained renderer-side phase. | Structural/narrow regression suite and bounded cost characterization required by #59 | Reintroduced whole-surface copies, hidden rollback snapshots, or second mutable cache audit | #59 performance record plus exact phase-execution diagnostics | M6A | owner-accepted | Required |

## M6B — canonical paint/hit scene kernel and displayed-hit cutover

| ID | Required observation | Positive proof owner | Negative proof owner | Diagnostic / trace proof owner | Delivery slice | Status | Gate |
|---|---|---|---|---|---|---|---|
| SCENE-OWN-01 | `PaintPublication`, its reusable `PaintScene`, `HitTestScene`, layout, semantics, and diagnostics are distinct immutable authoritative products aligned by one `SurfacePublication`; the paint wrapper owns renderer update identity/metadata without becoming authority for hit/layout/semantic facts. | Public complete-publication product/type conformance | Cross-product field/type leakage and duplicate-authority audit | Publication alignment diagnostics | M6B | owner-accepted | Required |
| SCENE-OWN-02 | Core owns only renderer/host-neutral paint and hit contribution values required by the M6B kernel; runtime alone composes surface scenes, injects mounted region targets, records exact displayed-snapshot mounted-target membership, composes layout-to-surface placement, retains displayed hit generations, allocates the M6B paint revision identity required to distinguish renderer updates from input generations, and commits publication state. M6B introduces no resource reference/provider, raster-scale, damage, scene-requirement, capability-negotiation, payload lookup, decoding/shaping, or resource/backend cache authority; those positive contracts remain M6C work under ADR 0007. | Core/runtime dependency, downstream paint/hit contribution, scene-composition, paint-revision separation, and snapshot-membership proof | Runtime arena/backend/host/resource-provider/raster-scale/damage/capability authority in core; provider registry/lookup/payload/cache authority in core/runtime; and downstream live-target/surface-placement/revision/membership construction compile/API proof | Repository authority audit | M6B | owner-accepted | Required |
| SCENE-OWN-03 | `SurfacePublication` exposes the exact immutable `PaintPublication` and `HitTestScene` committed with layout/semantic/diagnostic siblings; paint scene/metadata access derives from the paint publication, `SurfaceInputContext` and exact displayed-generation mounted-target membership are stored by the hit scene and exposed read-only through that product, and consuming one product transfers no live runtime mutation authority. | Public publication access/consumption/context/membership identity tests | Duplicated scene/metadata/context/membership storage, public revision/scene mutators, and detached-live-authority proof | Publication identity/alignment diagnostics | M6B | owner-accepted | Required |
| SCENE-OWN-04 | `PaintScene` data contains no semantic role/action/state tree, `WidgetTypeId`, concrete widget/control kind, mutable mounted state, backend handle, native host type, predecessor-relative damage/revision history, raster-scale mutation authority, provider/resolver object, payload store, callback, or separately mutable scene-requirements authority. | Genuine neutral scene consumer/API audit | Forbidden-type/history/scale/provider/resolver/requirements field compile/API audit and source scan | Repository scene-vocabulary audit | M6B | owner-accepted | Required |
| PAINT-01 | `WidgetPaintProof` is replaced by a state-aware public `PaintContribution` hook shared by built-ins and downstream widgets; contribution is renderer-neutral and action-type-independent. | Built-in plus genuine external-widget contribution proof | Registry/built-in special case, action coupling, and backend-type exclusion | Contribution validation diagnostics | M6B | owner-accepted | Required |
| PAINT-02 | Paint contribution is owner-local and receives only final local logical size/resolved style facts; runtime composes owner placement into self-contained scene local-to-surface transforms so consumers never recover placement from layout/mounted storage, and contribution receives no raster-scale or resource provider/lookup/cache authority. | Downstream custom-widget context plus public scene placement/scale/resource-authority exclusion proof | Absolute-surface/ID/backend/scale/provider/lookup authoring and hidden-layout reconstruction proof | Paint contribution/composition diagnostics | M6B | owner-accepted | Required |
| PAINT-03 | Basic filled and stroked logical rectangles validate finite geometry and exact literal color/stroke semantics: existing core `Color` is unpremultiplied sRGB8 plus linear alpha, M6B scene items have fixed opacity `1.0` and source-over composite in accepted order, zero-area rectangles produce no fill or stroke coverage, positive stroke width is centered as the exact local mitered ring between the rectangle expanded and inset by half-width (with a collapsed inset treated as empty), and width zero is no-op rather than a device hairline. Explicit per-item opacity remains M6C composition breadth under `PAINT-04`. | Built-in/downstream color/fill/stroke scene plus independent fixed-opacity logical-compositor and degenerate/miter-ring proof | Non-finite/malformed geometry, backend-default color reinterpretation, alternate blend mode, premature explicit-opacity interpretation, rounded/beveled/inside/outside-only stroke, negative width, degenerate-rectangle coverage, and zero-width hairline proof | Paint validation/composition diagnostics | M6B | owner-accepted | Required |
| HIT-01 | Widgets contribute zero or more owner-local hit regions through a distinct hit hook; default contribution is empty so layout participation alone never implies pointer targetability and region absence is the sole pass-through representation. | Button/custom-widget opt-in and generic container/text/omitted-region contrast proof | Every-layout-rect-targetable, paint-derived-hit, and explicit no-op pass-through region proof | Hit contribution diagnostics | M6B | owner-accepted | Required |
| HIT-02 | Widgets cannot author mounted hit targets, displayed-snapshot target membership, or surface placement; runtime injects the exact live `MountedNodeId` for each targetable region, records exact membership independently for every mounted target published in the generation, and composes owner placement for regions. | Runtime composition plus downstream contribution and no-hit membership proof | Forged/foreign/stale/arbitrary region target, authored membership, membership-from-hit inference, and absolute-placement construction proof | Mounted target/membership/placement integrity diagnostics | M6B | owner-accepted | Required |
| HIT-03 | The public immutable `HitTestScene` is the exact scene retained by runtime for displayed input. It contains two distinct runtime-composed facts: ordered pointer-hit regions and exact mounted-target membership for that published generation. Point resolution consults only regions; checked resolved-target ingress consults only exact snapshot membership before the inherited current mounted-target validation; membership never makes a node pointer-targetable and a region is never required for membership. No copied private rectangle pointer snapshot or `SurfaceFrame` hit implementation remains a competing pointer authority, while current directional-focus geometry is independently derived from the retained layout phase and never from hit-region participation. | Public-scene/runtime point-resolution identity, resolved-target-on-no-hit-node, focusable-no-hit, and hit-only-nonfocus proof under inherited M4 surface/directional corpora | Deliberately divergent duplicate-pointer scene, private rectangle resolver, membership/hit coupling, current-tree membership retarget, hit-scene-as-focus-geometry, and pointer-policy-affects-focus tests/source audit | M4 pointer/surface trace plus unchanged M4 focus-selection trace and scene-selection/membership diagnostics | M6B | owner-accepted | Required |
| HIT-04 | `SurfaceInputContext::hit_test_generation` remains the sole public displayed input-scene generation, and current/retained scene lookup preserves accepted M4 `SURFACE-01..12` behavior: historical point resolution uses that scene's regions, historical resolved-target validation uses that scene's exact mounted-target membership, retired/missing/foreign/mismatched contexts do not retarget through current state, one current logical surface does not imply multi-window/cross-surface focus lifecycle, and paint revision remains a distinct renderer-only authority. | Full inherited M4 surface-context corpus against `HitTestScene` retention plus no-hit resolved-target membership and unchanged-paint/different-hit generation contrast | Independent hit-scene counter, paint/input identity conflation, generation/membership mismatch, current-geometry/current-membership retarget, retired-scene reuse, and premature multi-surface lifecycle inference proof | Inherited M4 `SURFACE-*` trace evidence plus scene generation/membership diagnostics | M6B | owner-accepted | Required |
| HIT-05 | Public `WidgetInvalidation::HIT_TEST` exists, participates in `ALL`, invalidates hit contribution, and dirties hit composition; layout/structure imply required hit work while paint/semantics alone do not. | Invalidation dependency/phase-count tests including state-only M6B region geometry/existence changes | Missed hit update, premature M6C policy/layer coupling, and undocumented PAINT/SEMANTICS coupling proof | Surface phase report plus hit invalidation diagnostics | M6B | owner-accepted | Required |

## M6C — composition semantics, resources, metadata, damage, and capabilities

| ID | Required observation | Positive proof owner | Negative proof owner | Diagnostic / trace proof owner | Delivery slice | Status | Gate |
|---|---|---|---|---|---|---|---|
| PAINT-04 | Every paint item exposes validated finite local-to-surface transform, explicit clips each with its own finite clip-to-surface transform, finite opacity in `[0,1]`, and snapshot-local layer; zero clips mean no clip restriction, multiple clips combine only by intersection, a non-invertible item transform produces no paint coverage, a non-invertible clip excludes coverage for that item, invalid opacity is rejected rather than clamped, opacity participates in the accepted source-over color contract, and representation cannot contain an unbalanced transform/clip/opacity stack. | Nested transform/multiple-clip/singular-transform/opacity/color snapshot fixtures | Clip union/last-wins/order dependence, singular-transform fallback or clip disappearance, non-finite transform, out-of-range opacity, alternate opacity/color interpretation, malformed clip, hidden-layout transform, and stack-underflow/overflow exclusion | Paint item validation/composition diagnostics | M6C | owner-accepted | Required |
| PAINT-05 | Global paint order is exactly stable ascending `(layer, mounted logical preorder, contribution-local order)`, independent of map/storage/backend iteration. | Reorder/layer/equal-key deterministic scene corpus | Hash/storage iteration, alternative tie-break, and backend-resort dependence proof | Scene order diagnostics/snapshot comparison | M6C | owner-accepted | Required |
| PAINT-06 | Image and shaped-text-run primitives carry self-disambiguating logical resource references and exact logical placement. An image maps its complete normalized resource domain `(0,0)..(1,1)` affinely to the destination logical rectangle with no implicit crop/contain/cover/repeat mode. A shaped run maps resource-local `(0,0)` to its finite logical placement point, preserves the resource's authored local glyph/coverage geometry, and carries an ordinary literal core `Color` as its scene-owned monochrome foreground; changing only that foreground changes paint scene content without requiring a new shaped-run `ResourceRef`. No implicit fit, baseline guess, scaling, reshaping, or reflow occurs. M6 performs no image decode, glyph shaping, font discovery, upload, resource lookup, or backend realization. | Deterministic reference/placement consumers proving full-image mapping, shaped-run local-origin placement, and same-resource/different-foreground scene updates | Raw backend handle/native image/font/shaper dependency, backend-selected crop/fit/repeat, hidden baseline/origin inference, implicit text scaling/reflow, foreground encoded into shaped-resource identity, and placement/color divergence audit | Resource reference/placement/requirement diagnostics | M6C | owner-accepted | Required |
| HIT-06 | Hit regions and clips use the same explicit coordinate/shape contract: finite local-to-surface region transforms and independently transformed clips; `LogicalRect` remains left/top-inclusive and right/bottom-exclusive; four circular rounded-corner radii are finite/non-negative and normalized by one exact proportional factor so adjacent sums fit width/height; public consumers and runtime resolve transformed/clipped rounded points identically without consulting layout storage. | Oversized/equal/zero radius, half-open boundary, transform/clip/rounded hit corpus | Backend-specific radius clamp, hidden-layout reconstruction, edge-inclusion mismatch, untransformed fallback, clip bypass, and paint-geometry substitution proof | Pointer resolution trace plus hit shape/evaluation diagnostics | M6C | owner-accepted | Required |
| HIT-07 | A non-invertible region transform is non-hittable; a non-invertible clip transform excludes the region for that clip; neither case falls back to untransformed geometry, removes the clip, or retargets another owner silently. | Singular region/clip transform fixtures | Fallback/NaN/undefined inverse, clip disappearance, and lower-order corruption proof | Hit transform diagnostic plus no-route/next-region trace | M6C | owner-accepted | Required |
| HIT-08 | Hit order is exactly stable ascending `(layer, mounted logical preorder, contribution-local order)` with topmost defined by reverse traversal; `PointerPolicy::{Target, Block}` has exact first-containing semantics; region presence is the physical visibility fact; omitted regions are the sole non-blocking/pass-through representation; `Block` is the sole containing non-targeting/occluding representation; no redundant per-region visible/inert boolean exists; and physical hit policy is not implicitly derived from semantic disabled/hidden/inert state. | Overlap/order/policy/omission/visibility plus explicit physical hidden/inert lowering and disabled-control routing corpus | Alternative tie-break, explicit no-op pass-through policy, redundant visible/inert state, semantic-state coupling, blocked-through-target, first/last ambiguity, and storage-order dependence proof | Pointer resolution/default trace plus hit policy/order diagnostics | M6C | owner-accepted | Required |
| SCENE-RES-01 | Core-owned `ResourceRef` carries explicit neutral kind plus opaque self-disambiguating identity whose equality includes whatever provider/domain namespace is required to prevent concurrently live collisions. Once issued for a live provider/runtime domain, a ref remains stable by value and bound to the same immutable logical renderer content for that domain lifetime; logical content replacement requires a new ref; dropping backend realization caches cannot rebind the logical ref; refs remain distinct from mounted/semantic/surface identity, bytes, provider objects, scene-local indices, and backend handles. For the M6 shaped-run kind, resource identity covers immutable shaped glyph/coverage geometry and non-paint metadata but not the primitive's literal foreground color. | Multi-provider same-local-key collision, stable live-domain identity/content replacement, backend-cache-drop identity preservation, shaped-run same-ref/different-foreground, kind, and reference-comparison proof | Namespace collision/guessing, mutable/rebound logical content behind same live-domain ref, foreground absorbed into shaped-resource identity, identity substitution, scene-local reindex, raw bytes/backend handle, and kind-confusion proof | Resource identity/lifetime diagnostics | M6C | owner-accepted | Required |
| SCENE-RES-02 | M6 deliberately exposes no public RunenUI resource lookup/resolver/provider-registration/payload-byte/view/lease/cache API. Runtime and neutral consumers treat the whole `ResourceRef` atomically; resource kind contributes deterministic derived scene requirements/capability checks; an unsupported kind is rejected as an unsupported requirement rather than reinterpreted; consumers never split a local key to choose/guess a provider and no hidden RunenUI resource cache is required. Production lookup/storage/decoding/shaping/realization is deferred beyond M6 without changing M6 reference identity. Fixture-only payload maps may exercise consumer code but remain external test data, not protocol authority. | Public reference/requirement/capability comparison plus fixture-only consumer proof | Public M6 resolver/provider registry/payload view/lease API, runtime lookup/cache, split-key provider selection, silent fallback/widget-kind reinterpretation, and fixture map becoming public protocol proof | Resource requirement/capability diagnostics | M6C | owner-accepted | Required |
| SCENE-META-01 | Public runtime `RasterScale` is finite and strictly positive with deterministic default `1.0`; the existing public `SurfaceBuildContext` is the sole neutral M6 raster-scale input, and the exact accepted value is exposed read-only through `PaintPublication` together with validated logical surface size. The size defines the renderer target logical canvas/full-surface rectangle, scale never changes logical coordinates, widgets/scenes/renderers cannot override scale, and later native host integration supplies native scale through the same neutral context without native types leaking into M6. | `RasterScale` validation plus ordinary `SurfaceBuildContext` 1.0/2.0 publication/consumer comparison, full-surface extent, unchanged-scene/new-scale revision, and logical hit invariance proof | Zero/negative/non-finite scale, widget/paint/renderer scale setter, hidden-layout extent lookup, physical-type API leak, and scene-geometry rescaling proof | Surface-build and paint-publication metadata diagnostics | M6C | owner-accepted | Required |
| SCENE-META-02 | Damage is deterministic logical metadata from the exact `base_revision` to current `PaintRevision`: first publication and every logical-size/`RasterScale` change have full-surface damage; other changed paint publications never under-report renderer-relevant differences; conservative full damage is allowed; unchanged renderer tuples create no new paint publication/revision; damage never changes `PaintScene` content identity. | Initial-publication, size/scale change, different-predecessor/same-scene, clean/reused-publication, narrow, and full-damage deterministic corpus | Underdamage, fabricated empty update, undefined full-surface extent, history-dependent scene equality, and damage-in-content proof | Damage/publication diagnostics | M6C | owner-accepted | Required |
| SCENE-META-03 | Scene requirements are exactly derivable from canonical `PaintScene` content, including resource kinds, and are not separately mutable/versioned publication authority; external neutral consumer capabilities are checked against that derived view without backend-specific scene rewriting, semantic fallback, or resource lookup. | Requirements recomputation/cache-equivalence plus resource-kind capability acceptance/rejection and unchanged-scene/different-consumer fixture proof | Stale requirements cache, independent requirements version, alternate-backend scene, capability-in-scene identity, hidden lookup, silent lowering, and widget-kind fallback proof | Unsupported-requirement/cache-integrity diagnostics | M6C | owner-accepted | Required |
| SCENE-META-04 | `PaintPublication` carries exact `SurfaceId`, non-zero non-wrapping `PaintRevision`, and optional exact `base_revision`: revision `1`/no base is first; a new revision is allocated iff `(PaintScene content, logical surface size, RasterScale)` changes; unchanged renderer state reuses the same immutable paint publication/revision; each changed publication bases on the immediately previous accepted paint revision; consumers use damage incrementally only when their realized `(SurfaceId, PaintRevision)` matches that base and otherwise reprocess the complete RunenUI scene snapshot; required revision exhaustion fails before commit while unchanged paint consumes no revision. Full-resync requires no hidden prior RunenUI scene/cache state; external resource payloads remain named only by stable refs under `SCENE-RES-*`. | First/change/semantic-only/hit-only reuse, scale-change, contiguous update, skipped-update/full-scene-reprocess after dropping prior RunenUI consumer state, cross-surface, and final-counter boundary corpus | Spurious non-renderer revision bump, reused revision for changed tuple, mismatched-base damage use, hidden prior RunenUI scene/cache dependence, cross-surface revision conflation, wrap/saturation, and exhaustion partial-commit proof | Paint revision/base/damage publication diagnostics plus inherited atomic-failure evidence | M6C | owner-accepted | Required |

## M6D — independent consumers, migration, and milestone closure

| ID | Required observation | Positive proof owner | Negative proof owner | Diagnostic / trace proof owner | Delivery slice | Status | Gate |
|---|---|---|---|---|---|---|---|
| SCENE-CONS-01 | Two independent deterministic consumers process the same public paint/hit products and agree on paint revision/base handling, literal fill/stroke/shaped-run color and source-over/stroke semantics, order, coordinate/transform/clip/rounded-shape semantics, image/shaped-run placement, point-hit outcomes, exact retained mounted-target membership for resolved ingress, resource-reference identity/kind/requirements/capability handling, and publication metadata interpretation within their declared capability sets. | Reference consumer plus genuine independent consumer corpus | Shared private implementation/hidden test seam, fill/stroke/text-color, clip/resource-placement/membership/reference/requirement divergence, base-mismatch damage misuse, and other divergent semantics proof | Cross-consumer comparison diagnostics | M6D | blocked | Required |
| SCENE-CONS-02 | At least one genuine downstream/custom renderer consumer renders or deterministically interprets ordinary `PaintPublication`/`PaintScene` without importing/branching on `Button`, `WidgetTypeId`, semantic roles, mounted storage, layout storage, or private runtime types and can full-resync from a non-contiguous revision without hidden prior RunenUI scene/cache state. Resource refs are processed symbolically/forwarded according to kind and declared capabilities, or unsupported resource kinds are rejected deterministically; no private runtime provider/lookup bridge is permitted. | External renderer package/reference app full-scene-reprocess and resource-reference/capability proof | Concrete-control match, provider/lookup registry, semantic renderer input, hidden layout lookup, hidden prior RunenUI scene/cache dependence, input-generation coupling, and private dependency audit | Downstream consumer/resource-requirement diagnostics | M6D | blocked | Required |
| SCENE-CONS-03 | `runenui_testing` inspects/asserts the ordinary latest public paint/hit products and derives point input from exact public context without fabricating paint revisions/base lineage, raster scale, scenes, metadata history, targets, input generations, resource provider/lookup state, or parallel hit logic. Fixture payload maps, if used to exercise downstream consumer code, remain external test data and cannot become RunenUI protocol state. | Public `TestHarness` scene/metadata/revision assertion and input convergence tests plus external fixture-data separation proof | Private bridge, forged identity/history/revision/scale/provider/lookup, duplicate hit algorithm, and parallel expected-runtime proof | Harness/public runtime/consumer trace comparison | M6D | blocked | Required |
| SCENE-MIG-01 | `WidgetPaintProof`, its mounted capability cache, public re-exports, downstream uses, and compatibility aliases are removed once `PaintContribution` is live. | Removed-symbol/public API/downstream migration audit | Deprecated/type-alias/wrapper/doc-hidden compatibility scans | Repository migration audit | M6D | blocked | Required |
| SCENE-MIG-02 | `SurfaceFrame`/`SurfaceNode` no longer claim renderer paint or hit-test authority; any surviving layout/debug product has a separately truthful purpose and cannot be selected accidentally as the renderer/hit protocol. | Public API/documentation and consumer migration proof | Paint/hit methods, mixed widget-proof fields, and renderer-selection compatibility audit | Repository authority audit | M6D | blocked | Required |
| SCENE-MIG-03 | The private copied `HitTestSnapshot`/rectangle resolver and any duplicate debug/test pointer-hit algorithm are removed only after point resolution and exact historical resolved-target membership validation both use canonical `HitTestScene`, while current directional-focus geometry uses a runtime-private projection of the same retained layout phase; explicit hit participation never changes snapshot membership or M4 focus eligibility/geometry, and the complete inherited M4 surface/directional-focus corpora remain green. | Source/public integration, resolved-target-on-no-hit-node, focusable-no-hit/hit-only-nonfocus migration proof, and full accepted `SURFACE-*`/`DF-*` corpus rerun | Parallel pointer snapshot/resolver, membership-from-regions, current-state retarget, duplicate focus cache, focus-through-hit-scene, stale current-focus geometry, and deliberately divergent resolver/search audit | M4 pointer/surface trace plus unchanged M4 focus-selection/scroll trace and M6 hit-scene diagnostics | M6D | blocked | Required |
| SCENE-MIG-04 | Current docs/status/support/public API and downstream examples describe accepted M6 products truthfully; no backend/native/semantic-widget support is claimed, no proof-era scene authority remains, and integrated M4/M5/M6 validation is green. | Cross-document truth audit, downstream examples, full configured matrix audit | Stale symbol/support claim, premature M7+/backend claim, duplicate matrix ID/status audit | Repository validation authority and M6 closure record | M6D | blocked | Required |

## M6 closure rule

M6 closes only when all 36 rows are `owner-accepted`, all inherited M4/M5
validation remains green, #59 is completed under the accepted M6A substrate,
proof-era paint/hit authorities assigned above are removed, two independent
scene consumers pass, and final current-contract reconciliation is accepted and
validated on `main`.
