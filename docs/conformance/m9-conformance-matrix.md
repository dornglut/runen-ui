# M9 Conformance Matrix

> **Category:** Target architecture
>
> **Status:** M9A, M9B, and M9C owner-accepted; M9 complete
>
> **Milestone:** M9
>
> **Reviewed baseline:** `68808fb1dd5bfbd644a74fcfe916870428bcdd59`
>
> The accepted M9 target contract remains normative. M9A is normative for its ten
> owner-accepted visual/composition rows, M9B is normative for its ten owner-accepted
> deterministic-motion rows, and M9C is normative for its five owner-accepted
> integrated-production rows after their accepted implementations and bounded
> current-truth reconciliations.

[ADR 0010](../adr/0010-visual-composition-and-animation.md) owns M9 architecture.
[ADR 0011](../adr/0011-m9-path-fill-shadow-spread-clarification.md) narrowly amends
ADR 0010 only for path fill/stroke closure, ellipse degenerate/boundary behavior,
and ordinary shadow spread/effect-bound semantics.
[ADR 0012](../adr/0012-m9-group-ordering-clarification.md) narrowly amends ADR 0010
only for composition-group ordering, grouping scope, node-effect isolation/group
lifetime, and the exact relationship to inherited M6 `PAINT-05`.
[ADR 0013](../adr/0013-m9-node-decoration-publication-clarification.md) narrowly
amends ADR 0010 only for common node background/outline decoration geometry,
runtime publication ownership, and inherited M6 ordering.
[ADR 0014](../adr/0014-m9-degenerate-stroke-and-primitive-compositing-clarification.md)
narrowly amends ADR 0010 and ADR 0011 only for zero-extent rectangle-family stroke
geometry, point-degenerate path fill/stroke geometry and limiting tangents, and
one-item compositing under disposable renderer tessellation.
[ADR 0015](../adr/0015-m9-shadow-support-and-painter-order-clarification.md) narrowly
amends ADR 0010, ADR 0011, and ADR 0012 only for the neutral effect-support set used
by ordinary shadows/nested groups, its relationship to alpha and conservative bounds,
and exact ordinary-shadow painter order.
[ADR 0016](../adr/0016-m9-motion-identity-and-interpolation-clarification.md) narrowly
amends ADR 0010 only for deterministic motion declaration identity/arbitration,
transition-policy resolution, the initial motion-target/interpolation domains,
cubic-Bezier evaluation, and reduced-motion strategy validity/suppression.
[ADR 0017](../adr/0017-m9-zero-duration-forever-timing-clarification.md) narrowly
amends ADR 0010 and ADR 0016 for motion timing normalization only: zero-duration
forever timelines reject, finite relative schedules are checked independently from
candidate-start overflow, and accepted finite zero-duration behavior remains exact.
M3 owns mounted identity/lifecycle/invalidation; M4 owns logical time, scheduling,
wake/redraw, and trace; M5 owns semantic identity/publication/testing; M6 owns the
renderer-neutral paint/hit publication model and `ResourceRef`; M7 owns concrete
renderer/host/resource edges; M8 owns production style/layout/text and current
property-effect classification. This matrix adds only new M9 observations.

```text
25 total unique rows
25 owner-accepted
0 implementation-complete
0 proof-complete
0 blocked
0 duplicate IDs
0 invalid statuses
0 invalid schemas
```

All rows are `Required`. Serial M9 delivery is M9A visual/composition production,
M9B deterministic transitions/timelines, then M9C integrated closure.

A dependency-owned public path/paint vocabulary, retained second scene/tree,
renderer-owned animation clock/timeline, per-frame action queue, stale hit/semantic
geometry under visual motion, ambient preference policy, custom backend shader escape
hatch, software expected renderer, cache/revision counter acting as behavior authority,
or compatibility shim preserving replaced pre-1.0 visual authority cannot satisfy M9.

## M9A — production visual/composition vocabulary and renderer realization

| ID | Required observation | Positive proof owner | Negative proof owner | Diagnostic / trace proof owner | Delivery slice | Status | Gate |
|---|---|---|---|---|---|---|---|
| M9VIS-01 | Public visual/composition vocabulary is RunenUI-owned and responsibility-normalized: common node background-brush/outline/shadow/opacity/presentation policy resolves through computed style/runtime; common background and outline use one runtime-derived final owner-local layout-box/radius decoration shape and are synthesized into immutable publication rather than reinterpreted by built-in widgets; arbitrary shape/path/image content remains owner-local paint contribution; composed transforms/groups/effect bounds remain immutable runtime publication. No Lyon, Kurbo, Peniko, Vello, wgpu, WGSL, native, mounted, or dependency scene type becomes public authority. | Public-API/style/node-decoration/paint/publication ownership corpus | Duplicate common-decoration authority, widget-private background/outline interpretation, forbidden-type/public-dependency, item-transform-as-node-presentation, and second-scene audit | Visual schema/provenance/decoration-ownership report | M9A | owner-accepted | Required |
| M9VIS-02 | Validated rect/rounded-rect/ellipse/path geometry has finite logical coordinates, explicit contour/fill-rule semantics, deterministic logical bounds, and fill-hit behavior independent of renderer tessellation. Each `move` starts a contour; move-only contours have no coverage; every structurally segment-bearing contour is logically closed for fill by a synthetic final-to-first edge when no authored `close` exists. Point-degenerate authored line/quadratic/cubic segments remain structural/validation content but contribute no fill boundary or winding change; a contour with no non-point-degenerate segment therefore has no geometric fill coverage even though it remains structurally segment-bearing. Authored/synthetic non-point-degenerate fill boundaries are inside coverage; non-boundary points use the authored non-zero/even-odd rule. Ellipse zero width/height is empty and positive-extent analytic ellipse boundary is inside without revising inherited M6 half-open rect/rounded-rect semantics. Structural path equality/content is RunenUI-owned; logical containment remains core-owned, and dependency flattening tolerance/degenerate defaults cannot become public semantics. Malformed contour verb ordering rejects and paint never implicitly manufactures a physical hit target. | Shape/path/open-fill/point-degenerate/boundary/ellipse/equality/bounds/hit/core-adapter corpus | Non-finite/malformed/move-only path misuse, missing open-fill closure, point-degenerate dependency geometry, dependency/raster/tessellation/tolerance-derived containment, allocation/dependency identity, ellipse-degenerate drift, and paint-derived-hit audit | Path contour/validation/point-degenerate/bounds/hit/boundary/adapter records | M9A | owner-accepted | Required |
| M9VIS-03 | Stroke semantics use one centered-on-boundary placement with finite non-negative width; butt/round/square caps; miter/bevel/round joins; finite miter-limit ratio >= 1 with deterministic bevel fallback; zero width has no coverage and is never a backend hairline. Inherited M6 zero-area rectangle stroke emptiness is preserved, and a zero-width/zero-height rounded-rectangle outer box likewise has empty stroke coverage rather than dependency-manufactured line/capsule/point geometry. Point-degenerate authored line/quadratic/cubic segments remain structural path content but contribute no fill boundary, stroke centerline, or cap/join tangent; geometric stroke topology skips them while preserving authored open/closed contour state. For non-point-degenerate quadratic/cubic endpoints whose first derivative vanishes through coincident controls, the cap/join tangent is the first non-zero endpoint chord in polynomial control order, i.e. the limiting polynomial direction. An authored `close` adds the real final-to-first centerline edge when non-point-degenerate and always preserves closed-seam topology; an open contour retains two capped endpoints at its first/last non-point-degenerate segments and its synthetic fill-closing edge is never stroked. A second `close` or segment after close without a new `move` rejects. Logical bounds include complete accepted cap/join/closing-edge coverage; inside/outside stroke is not a hidden backend option. One logical stroke item is one compositing source: disposable tessellation overlap within that primitive cannot multiply its brush/item opacity or perform repeated source-over at one covered sample. | Stroke geometry/open-vs-closed/seam/cap/placement/bounds/point-degenerate/tangent/single-item-overlap corpus | Synthetic-fill-edge-as-stroke, backend-default/hairline/inside-outside/invalid-miter, zero-area rectangle-family coverage, point-degenerate dependency caps/joins, dependency tangent/cusp recovery, tessellation-overlap-as-repeated-source, invalid post-close continuation, and truncated-bound corpus | Stroke contour/closure/normalization/placement/bounds/degenerate-tangent/compositing records | M9A | owner-accepted | Required |
| M9VIS-04 | Solid, linear-gradient, and concentric radial-gradient brushes preserve primitive-local logical geometry, nondegenerate linear endpoints / positive radial radius, stable nondecreasing stops with deterministic hard-stop side semantics and endpoint extension, straight-alpha sRGB8 inputs, and premultiplied linear-sRGB interpolation without backend-specific color policy; the M8 color-only background is cleanly replaced by one brush-valued runtime node background over the ADR 0013 final-local-box/radius decoration shape rather than retained as widget-private or parallel authority. | Brush/gradient/color/style/node-background-radius-cutover corpus | Degenerate-gradient, unsorted/non-finite stop, hidden color-space, backend-brush, widget-private background, and duplicate-background audit | Gradient normalization/interpolation/style/decoration provenance records | M9A | owner-accepted | Required |
| M9VIS-05 | Image ordinary-fit geometry uses exact non-zero neutral intrinsic dimensions supplied by the same logical resource owner as the complete opaque `ResourceRef`, plus a positive normalized source crop, finite normalized alignment, and logical destination: `fill` independently scales axes, `contain` uses min uniform scale, `cover` max uniform scale, `none` scale 1, and `scale-down` chooses none when fitting else contain. The M7 provider payload extent for that same ref must exactly equal the descriptor intrinsic extent; mismatch is a structured resource/realization failure rather than renderer choice, hidden rescale, or ref remint. Runtime/publication resolves exact source/destination geometry; fit/crop/layout/raster/device state never remints `ResourceRef`. | Image descriptor/provider-correlation/fit/crop/alignment/resource-identity corpus | Invalid-crop/alignment, missing metadata, descriptor/provider extent mismatch fallback, renderer-fit, hidden rescale, fit-key/resource-remint, and scale/device-bound identity audit | Image geometry/resource/provider correlation and mismatch records | M9A | owner-accepted | Required |
| M9VIS-06 | Nine-slice is a distinct initial image mode with exact source rect/insets, logical destination edge widths, proportional normalization when opposing edges exceed destination extent, non-negative center, stretched corners/edges/center under the accepted axis rules, no implicit tiling, and the same complete image `ResourceRef`. | Nine-slice geometry/mapping corpus | Fit/nine-slice ambiguity, tiling fallback, negative-center, overlapping-source, and reminted-resource corpus | Slice normalization/placement records | M9A | owner-accepted | Required |
| M9VIS-07 | Immutable snapshot-local composition groups preserve nesting and exact ordered membership while qualifying inherited M6 `PAINT-05` only where atomic isolation makes outside interleaving impossible. Runtime first derives the exact stable global M6 pre-group order `(layer, mounted logical preorder, contribution-local order)`. ADR 0013 node background/outline are ordinary node-owned `SceneLayer::ZERO` items: for the same owner at layer zero local order is background -> widget-authored items in exact relative order -> runtime-shaped text -> outline, while non-zero widget layers continue to obey the same global M6 ordering key and decoration gains no always-back/front escape. Runtime-synthesized decoration and runtime-shaped text remain outside widget-authored explicit owner-local groups; enclosing runtime node-effect groups may capture them only through accepted mounted-subtree scope. Every item has at most one immediate group and group parentage is single-parent/acyclic; sibling group descendants may interleave in pre-group order. Each non-empty group anchors at its earliest descendant pre-group item and recursively contracts to one atomic parent entry there; with no groups M6 order is unchanged, and empty groups have no coverage/effects and may be omitted. Descendant `SceneLayer` values remain pre-group ordering facts inside an isolated group but cannot escape that ancestor group and re-enter parent ordering after contraction. Primitive/decorative geometry uses item-local transform -> node presentation transform -> final owner placement; contribution clips retain independent clip-local-to-owner transforms, then receive node presentation + owner placement. Each clip list remains conjunctive exactly as M6 requires; structural group scopes accumulate outer-to-inner. Item clips constrain items; group children source-over compose; group effects derive from composed coverage; group clips constrain the complete group result/effects; group opacity applies once before source-over into the parent at the frozen first-member anchor. Groups acquire no mounted/lifecycle/semantic/cross-publication authority. | M6 pre-group order/node-decoration local-order/explicit-group non-capture/transform/clip-intersection/nested-group/first-member-contraction/sibling-interleaving/cross-layer-non-escape/empty-group composition corpus | Renderer/group-layer resort, decoration always-back/front bypass, runtime-decoration capture by widget explicit group, descendant-layer escape, transform-order swap, item-transform leakage into clips, clip-list-order-dependent coverage, per-child-opacity substitution, malformed group parentage, backend blend-policy, and retained-second-tree audit | Pre-group order/decoration-order/group-membership/transform/clip/group structure/anchor/contraction records | M9A | owner-accepted | Required |
| M9VIS-08 | Ordinary node opacity and drop shadows use runtime-derived composition groups over the node's composed mounted visual subtree: descendants do not inherit the style property but their paint contributes to ancestor group coverage, and independently affected descendant nodes form nested groups. Outside active motion, the node-effect group exists exactly when effective node opacity is not `1` or the ordered ordinary-shadow list is non-empty; default opacity `1` plus an empty list does not isolate every node, while a non-empty shadow list remains an ordering fact even if its visual result is empty/transparent and may not be optimized away. Group opacity applies exactly once; ADR 0013 node background/outline are ordinary child paint over the shared final-local-box/radius decoration shape, contribute through ordinary item geometry to enclosing pre-shadow support, and outline does not itself create isolation. Ordinary-shadow geometry uses one ADR 0015 neutral effect-support authority separate from realized RGBA alpha: accepted fill/stroke geometry, resolved image destination patches, and exact retained shaped-text glyph-outline support contribute after transforms/item clips; brush/foreground alpha, image payload alpha, item opacity, antialiasing, MSDF/atlas samples, raster scale and cache/device state do not redefine that support. Direct child item/group support unions into one exact pre-shadow set `C`; every sibling shadow derives independently from that same `C`. ADR 0011 signed spread remains rotation-invariant Euclidean morphology: positive spread is dilation by a closed disk of radius `s`, negative spread is the corresponding disk erosion, zero spread preserves support, and complete erosion is empty. Offset follows spread; ADR 0015 freezes neutral blur support as the spread-adjusted/offset set Minkowski-summed with the closed axis-aligned square `[-3*sigma, 3*sigma]^2`, which is the finite blur-support envelope and not a replacement spread kernel. Group neutral output support is the union of child plus every shadow support, then conjunctive group clips apply; group opacity and shadow color alpha do not shrink that propagated support. Visually, shadows source-over in authored order behind the composed child result: first authored paints first, later shadows paint above earlier shadows, and children paint above all ordinary shadows before group clips/opacity. Nested groups propagate their clipped neutral child-plus-effect support rather than intermediate target alpha. Framework effect/damage bounds remain deterministic conservative logical AABBs enclosing this exact support; positive spread expands conservative pre-shadow bounds by `s`, zero/negative spread may conservatively retain them when tighter erosion bounds are unavailable, then offset and `3 * sigma` expansion apply. Off-surface sources/effects cannot be discarded before downstream effect derivation when their accepted support can reach the final surface or an ancestor effect. Renderer raster/kernel/cache/target choices cannot redefine subtree scope, support, spread, painter order, group existence, grouping, or framework bounds. Opacity/shadows alter paint/effect/damage only and do not silently alter layout/hit/focus/semantic bounds or physical hit ordering. | Node-decoration/subtree/static-group-existence/identity-default/visually-empty-shadow/nested-effect-group/group-opacity-once/neutral-effect-support/image-text-support/shadow-order/inherited-coverage/spread-rotation/erosion/blur-envelope/off-surface/effect-bounds corpus | Property inheritance as effect scope, own-item-only node effect, decoration-as-isolation, identity-groups-every-node, transparent-shadow group elision, per-child opacity, source-alpha/payload-alpha/MSDF-alpha/blur-alpha as shadow-geometry authority, sibling-shadow chaining, renderer-selected shadow order, shadow-over-child composition, axis-aligned-square spread/raster-kernel/backend-defined spread, orientation-dependent spread, infinite support, premature final-surface clipping, non-empty visual result after complete erosion, undersized logical bounds, invalid spread, target/cache-defined support, and semantic/layout/hit inflation audit | Node-decoration/group/subtree/group-existence/neutral-support/shadow-order/spread-operation/blur-envelope/off-surface/effect-bound/realization diagnostics | M9A | owner-accepted | Required |
| M9VIS-09 | The extension boundary remains explicit non-exhaustive neutral primitives/effects/resources; arbitrary shader/filter/backend callbacks, `Any` render nodes, device handles, or dependency-owned scene subtrees are not public escape hatches. | Extension/capability corpus | Shader/WGSL/wgpu-handle/custom-scene authority audit | Unsupported-capability diagnostics | M9A | owner-accepted | Required |
| M9VIS-10 | Lyon adoption uses only exact required private subordinate subcrates/patches after proof of RunenUI Rust 1.93 compatibility, features/transitives/licenses, build impact, and relevant shared corpus/benchmark. `runenui_core` may privately adapt `lyon_path`/`lyon_algorithms` behind RunenUI structural path values for logical geometry while dependency tolerance/identity remains non-authoritative; `runenui_render_wgpu` may use `lyon_tessellation` only for disposable fill/stroke realization and owns GPU/group/shadow caches reconstructible after cache/device loss. The Lyon meta crate is not adopted by default, and Kurbo/Peniko/Vello do not become parallel geometry/paint/renderer authorities merely for convenience. | Exact-dependency/MSRV/license/features/build + core-adapter + real-wgpu realization/re-realization corpus | Public-Lyon-type, Lyon-as-path-identity, renderer-owned logical containment, dependency-default semantics, unproved-patch, retained-Lyon-tree, second-geometry-model, Peniko scene, Vello/second-wgpu-renderer, and cache-as-authority audit | Dependency/adopt-build/core-adapter/renderer-cache records | M9A | owner-accepted | Required |

## M9B — deterministic transitions and timelines

| ID | Required observation | Positive proof owner | Negative proof owner | Diagnostic / trace proof owner | Delivery slice | Status | Gate |
|---|---|---|---|---|---|---|---|
| M9MOTION-01 | All animation sampling uses the accepted runtime `MonotonicClock`; one staged surface candidate observes one exact monotonic instant with no wall-clock read, renderer timestamp, sleep, background animation thread, or second clock. A new motion start instant is committed from that candidate time only when staging succeeds. | Manual-clock/same-sample/start-commit corpus | Wall-clock/renderer-time/second-clock and failed-stage-time-consumption audit | Sample/start instant records | M9B | owner-accepted | Required |
| M9MOTION-02 | Style transitions start only from canonical resolved target changes, including hover/focus/active/disabled state, under one non-inherited target-keyed transition policy resolved by the normal M8 style cascade. A layer may be absent, explicitly disable that target's transition, or provide one validated spec; explicit disable masks lower rules. The same-candidate winning policy governs a new target change. Policy-only change does not start inactive motion, while active-spec change replaces from the current sample toward the same target and active disable cancels to the governed target. Interruption/reversal always starts from the currently sampled presented value rather than stale prior target state. | Interaction/style-transition/policy-cascade/replacement corpus | Second transition cascade, inherited transition policy, absent-as-disable, competing policy, target-jump, stale-start-value, and policy-change-start audit | Target/policy-resolution/start/replace/cancel/sample provenance | M9B | owner-accepted | Required |
| M9MOTION-03 | Explicit timelines are declarative authored one-target values with owner-local unique `AnimationId`; one mounted owner may have at most one explicit timeline per `MotionTarget`, and duplicate IDs/target collisions reject before mutation rather than receiving order-based arbitration. Timelines have at least two strictly increasing normalized keyframes whose endpoints are exactly 0/1, one easing per segment, checked duration/delay, and repeat of one/non-zero finite total iterations/forever; delay applies once and initial M9 has no implicit autoreverse. Exact mounted-generation lifecycle preserves unchanged active/suppressed/**completed declaration** state across transient rebuilds, deterministically cancels/replaces/removes/unmounts/shuts down, and starts anew only after accepted absence/re-add or changed exact target/spec. Replacement explicit timelines begin from their own keyframe 0 rather than rewritten continuity. For one property, an active explicit timeline is the sole sampled owner and timeline termination resumes from its current/final sample toward the then-current style target under normal transition policy. | Keyframe/spec/ID-target-collision/reconciliation/completed-declaration/timeline-lifetime/same-property-precedence corpus | Duplicate ID, duplicate target, order-selected timeline, duplicate/missing endpoint keyframe, accidental completed-spec restart, hidden autoreverse, widget-local timer/state-machine, stale-generation, additive same-property animation, and competing transition audit | Spec/identity/collision/start/replace/cancel/completion/removal/re-add/lifetime/precedence records | M9B | owner-accepted | Required |
| M9MOTION-04 | Linear/cubic-Bezier easing and timeline normalization have exact endpoints, deterministic delay/iteration/zero-duration semantics, finite checked time arithmetic, and forever timelines never claim natural completion. For interior normalized progress, cubic-Bezier x inversion uses the accepted fixed `f64` algorithm: exactly 32 bisection iterations over `[0,1]`, updating `lo` only when `x(mid) < progress`, otherwise `hi`, then evaluating y at the final midpoint and clamping to `[0,1]`; dependency/Newton/tolerance-selected inversion is not authority. Ordinary samples are pure functions of retained endpoints/keyframes plus exact normalized progress and never incrementally integrate previous frame samples. Non-final iteration boundaries restart at keyframe 0; final boundaries sample keyframe 1 and complete. Zero-duration/zero-delay terminates in the same successful start candidate; zero-duration with delay holds start until its deadline. | Easing/fixed-bisection/timing/delay/iteration/zero-duration/history-independence corpus | NaN/out-of-range/overflow/phantom-frame, dependency/tolerance/Newton easing solver, ambiguous boundary/completion, frame-rate integration, and accumulated-rounding corpus | Normalized progress/easing-inversion/endpoint/sample/completion records | M9B | owner-accepted | Required |
| M9MOTION-05 | Motion targets are explicit typed derived overrides, never mutation of authored/application/widget state. Initial style targets are exactly foreground, background brush, padding, radius, typography, ordered shadows, opacity, and presentation; outline is not an initial motion target. Initial structural layout targets are exactly width/height, min/max width/height, margin, gap, flex grow/shrink, and flex basis; grid/container/alignment/positioning/overflow structure is not a target. Optional absence is a discrete endpoint and never implicitly becomes transparent/zero/identity. Continuous compatibility is exact: colors use premultiplied linear-sRGB with linear alpha; solid brushes interpolate color; linear/radial gradients interpolate stop colors only when kind, geometry, count and exact stop-offset/hard-stop structure match; opacity/padding/radius interpolate component-wise; presentation interpolates present-present translation/scale/raw-radians/origin component-wise with no shortest-arc or implicit pivot; shadow lists require equal length and pair by authored index; typography is wholly discrete. Numeric layout interpolation is only same-domain `Length`/`Percent` for `LayoutDimension`/`LayoutBound`/`FlexBasis`, component-wise margin/gap lengths, and linear flex factors; auto/intrinsic/content/cross-domain pairs are discrete. Sampled effective layout values feed the accepted runtime/Taffy/text authority, and generic numeric traits or arbitrary paint-item indices are never animation authority. | Property-target/closed-target-set/color/brush/shadow/presentation/layout-domain/compatibility/interpolation/effective-layout corpus | Generic-Lerp/reflection, hidden optional identity, shortest-arc rotation, gradient-geometry morph, shadow matching heuristic, typography interpolation/text-scale, unsupported layout-field motion, authored-state mutation, animation-specific layout engine, path/resource crossfade, and paint-index target audit | Motion-target/domain/compatibility/interpolation/discrete/effective-value records | M9B | owner-accepted | Required |
| M9MOTION-06 | Sampled property changes use exact direct-effect classification, transitive invalidation, and stage-local cache compatibility. Paint-only motion avoids layout/text; presentation motion updates paint/hit/focus/semantic geometry without relayout; layout motion recomputes layout/dependents; text-metric changes use the accepted text/layout authority. Node opacity/shadow motion recomposes paint/group structure under ADR 0012 without relayout: a live unsuppressed style transition keeps the node-effect group when either its current sampled effective value or terminal target requires isolation; a live unsuppressed explicit timeline keeps it when either its current sample or any accepted keyframe value in that live timeline specification requires isolation; a terminal post-completion identity state may remove it in the same staged candidate. Time advance alone does not invalidate stages whose exact effective inputs are unchanged or force invalidate-all. | Differential animated-invalidation/cache-reuse + transition/timeline group-lifetime corpus | Under-invalidation, motion-group renderer optimization, identity-keyframe/repeat group churn, suppressed-motion hidden grouping, identity-target premature group removal, time-invalidates-all, frame-counter cache key, renderer-text-scale, stale-product, and unconditional invalidate-all audit | Property-effect/dirty-phase/group-existence/cache-compatibility records | M9B | owner-accepted | Required |
| M9MOTION-07 | Mandatory M8 preference overrides remain above motion. Reduced-motion behavior derives only from explicit `StylePreferences::reduced_motion` and source-valid RunenUI strategy: transitions default to `SnapToEnd`, cannot use `HoldInitial`, and may explicitly preserve essential motion; finite timelines default to `SnapToEnd` and may use `HoldInitial`/`PreserveEssential`; forever timelines cannot use `SnapToEnd`, default to `HoldInitial`, and require `PreserveEssential` to continue as essential motion. Invalid source/strategy combinations reject. `HoldInitial` retains the authored timeline at keyframe 0 with no continuous redraw or hidden elapsed time and starts/restarts from the preference-change instant when reduced motion is disabled; `SnapToEnd` commits terminal value/lifecycle atomically. Conflicting motion masked by a mandatory **non-reduced** preference override contributes no motion-specific group requirement but its underlying same-clock lifecycle continues rather than pausing/restarting; if suppression lifts while still active, the current sample becomes visible. | Preference-precedence/strategy-validation/snap/hold/restart/non-reduced-suppression + group-authority corpus | Ambient platform/renderer policy, invalid source-strategy fallback, forever snap-to-end, transition hold-initial, motion-above-preference, suppressed-motion hidden stacking context, hidden speed multiplier, reduced-motion hidden time accrual, non-reduced pause/restart, redraw-while-held, and ignored-preference audit | Preference/strategy-validation/suppression/group-existence/restart/completion/cancellation records | M9B | owner-accepted | Required |
| M9MOTION-08 | Active visible motion uses existing redraw/wake authority; redraw acknowledgement may request another frame while motion remains active, but runtime does not enqueue one canonical FIFO action per sample or invent a fixed framework frame rate. Equal consecutive published samples may still request a future redraw while motion remains active without minting scene/resource identity. | Redraw/vsync/manual-time/equal-sample corpus | Per-frame FIFO, busy settle-loop, hidden 60-Hz timer, lost-redraw, and redraw-as-resource-identity audit | Redraw/wake/active-motion records | M9B | owner-accepted | Required |
| M9MOTION-09 | Live motion compatibility is exact mounted generation + target/source identity + exact accepted specification/endpoints/start instant/preference facts. Explicit declaration compatibility additionally includes owner-local `AnimationId`, exact target and exact timeline spec; unchanged completed declarations remain retained reconciliation state but no longer own sampled-property/redraw authority, accepted removal retires them, and later re-add starts anew. Replacement samples old live motion at the same candidate instant, orders cancel/replace before replacement start, uses that sample as continuity start only where the accepted source semantics allow it, and never rewrites explicit keyframe 0. Ordinary sampling does not mint a motion identity. Inherited product/publication revisions change only under accepted tuple/product rules, never time alone. Motion/declaration state, sample/terminal completion and ADR 0012 node-effect group existence commit atomically with staged products; invalid authored collisions, preference/strategy decisions, renderer failure, or retry cannot partially advance them, and retained renderer retry uses the exact already-sampled publication. | Motion/declaration-identity/completed-retention/revision/replacement-order/publication-failure/retry + group-existence atomicity corpus | Global frame/sample revision authority, completed-spec restart, stale terminal declaration, partial collision acceptance, explicit-keyframe rewrite, revision-on-time-only, partial generation, renderer-driven lifetime/completion, preference-suppression/group-state drift, retry-resample, and replacement/group-order ambiguity audit | Motion/declaration compatibility/product revision/cancel-start/completion/group-existence/candidate/commit/retry correlation | M9B | owner-accepted | Required |
| M9MOTION-10 | Animation inspection extends the canonical runtime diagnostics/trace with target/policy resolution, invalid duplicate-ID/duplicate-target rejection, start/replacement/suppression/restart/cancellation/completion/completed-declaration retention, easing/interpolation/discrete-domain decisions, reduced-motion strategy validation/preference decisions, and cache/effect decisions without creating a second mutable animation/cache log or requiring application actions to implement debug/tween traits. Initial completion emits no application action. | Trace/inspection/collision/compatibility/strategy/order corpus | Parallel event/cache ledger, silent collision arbitration, hidden interpolation fallback, completion-action side channel, debug-trait, and action-channel audit | Canonical motion trace records | M9B | owner-accepted | Required |

## M9C — integrated production closure

| ID | Required observation | Positive proof owner | Negative proof owner | Diagnostic / trace proof owner | Delivery slice | Status | Gate |
|---|---|---|---|---|---|---|---|
| M9INTEG-01 | One runtime-sampled node presentation transform uses the frozen scale -> rotate -> presentation-translate order about normalized origin, followed by layout owner placement, and is composed consistently into visible paint, physical hit testing, directional-focus geometry, semantic bounds, and presentation-relative clips. Item-local transforms remain paint-local; clip-local transforms remain independently owner-local. Hit uses exact transformed shape, focus/semantics use deterministic axis-aligned bounds of transformed authoritative geometry, and singular transforms have inherited empty-hit behavior rather than stale layout fallback. | Transform-order/paint-hit-focus-semantic/clip correlation corpus | Component-order drift, item-transform-to-clip leakage, paint-only presentation transform, stale hit/accessibility, and untransformed fallback audit | Node/sample/transform/geometry correlation records | M9C | owner-accepted | Required |
| M9INTEG-02 | Deterministic public/headless proof advances `ManualClock` explicitly and observes exact intermediate/end/delay/repeat/replacement/preference/cache/layout/text/semantic results through ordinary public runtime contracts without sleeps, private expected runtime, alternate scene engine, or software expected renderer. | Public manual-time integration corpus | Sleep/wall-clock/private-model/alternate-engine audit | Fixture/time/cache/publication diagnostics | M9C | owner-accepted | Required |
| M9INTEG-03 | Real-wgpu proof renders the same accepted neutral paths/strokes/gradients/images/nine-slice/groups/shadows and representative transition samples, including retained-publication retry and disposable cache/device re-realization, without renderer-owned style/timeline/hit/semantic authority or resource rebinding by raster/device state. | Real-wgpu visual/motion/contact-sheet/re-realization corpus | Vello/second-renderer, software expected renderer, renderer timeline, resource-rebinding, and retained-cache-authority audit | Runtime/scene/resource/renderer correlation records | M9C | owner-accepted | Required |
| M9INTEG-04 | Representative canonical hover/focus/active transitions visibly and deterministically exercise the M8 interaction/style path plus M9 transition path; no application-maintained hover state, widget timer, or showcase-only animation authority is required. | Interactive-state transition corpus | Duplicate hover state/showcase timer/direct renderer animation audit | Interaction-style-motion provenance | M9C | owner-accepted | Required |
| M9INTEG-05 | Final M9 authority cleanup removes/revises bounded M6/M8 assumptions superseded by accepted M9 production visuals/motion: color-only background, specialized public `FillRect`/`StrokeRect` primitive authority, the old exact-mapped image variant kept as a parallel path, duplicate shape APIs retained only for `Copy`, and any competing composition/motion path are removed/reconciled; `ShapedTextRun` remains the sole production text-paint authority. Current docs/API expose one truthful path with no compatibility alias, hidden renderer fallback, or obsolete proof authority preserved for convenience. | Source/API/current-truth cleanup corpus | Duplicate background/rectangle/image/shape/text/composition/motion/compatibility-authority audit | Repository authority/deprecation audit | M9C | owner-accepted | Required |

## Closure rule

M9 is conformance-complete only when all 25 rows are `owner-accepted` on accepted
default branch. Each serial implementation slice must be separately owner-accepted,
squash-merged, accepted-main validated, and followed by bounded current-truth/
conformance reconciliation before its rows become `owner-accepted`. M9 closes only
after final integrated reconciliation and accepted-main validation. Later M10/M11
work may consume the accepted visual/motion contracts but must not move their clock,
scene, property, hit, semantic, cache, resource, or renderer authority elsewhere.